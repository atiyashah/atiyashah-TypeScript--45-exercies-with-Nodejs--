/* T-Shirt write a function called make-shirt() that accept a size and the text of a message that should be printed
on the shirt.The function should print a sentence summarizing the ize of the shirt and the
message printed on it call the function*/
function makeShirt(size, printMessage) {
    console.log(" you selected ".concat(size, " size shirt with ").concat(printMessage, " print on shirt"));
}
makeShirt("Large", "codeWithAtiya");
makeShirt("small", "code with ATIYA");
